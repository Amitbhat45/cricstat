package com.example.cricstat.retrofitmatchlist

/*
class ImageFetcher(private val viewModel: ViewModelmatchList) {
    fun fetchImage(imageId: String) {
        viewModel.fetchImage(imageId)
    }
}*/
